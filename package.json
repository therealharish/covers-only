{
  "name": "covers-only-youtube-mix",
  "version": "1.1.1",
  "private": true,
  "description": "Store-ready Chrome extension that keeps YouTube Mix playback focused on covers.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "assets": "node tools/generate-icons.js",
    "package": "bash tools/package-extension.sh"
  }
}
